# Scenario Helpers

Use this folder for scenario setup helpers that create business test data through APIs or reusable UI preparation steps.

Recommended first helpers:
- `createClientScenario`
- `createResourceScenario`
- `createOpportunityScenario`
- `createSowScenario`
